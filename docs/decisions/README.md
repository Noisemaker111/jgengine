# Decision records

Permanent docs, one per settled engineering decision: what was decided, the context that forced it, and the consequences we accepted. Numbered in order (`0001-...md`), never rewritten after acceptance — a superseding decision gets a new file that names the one it replaces. Read these before re-opening an argument they already settled; write one when a session makes a call that future sessions must not re-litigate.
