Fan out: if this task has independent legs — separate subsystems, separate files, independent audits or verification — spawn one Opus subagent per leg now, in a single batch, instead of working them serially. Keep planning, overlapping edits, and synthesis in the main agent; never give two workers the same files; judge worker evidence, don't trust claims. Small edits, quick greps, and waiting stay inline.
