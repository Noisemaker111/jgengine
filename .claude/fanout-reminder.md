Cheap workers do the dumb work (`fan-out` skill). If this turn involves lint, typecheck, test, build, preview, screenshot, shoot, Playwright, GitHub ceremony, bulk reads, codebase scouting, research sweeps, renames, doc sweeps, or log triage — spawn a cheap Sonnet/Task worker; do not run those on the frontier model. Orientation is a scout job: never read breadth-first into unfamiliar code — send 1-3 Sonnet scouts that return scoped digests (file:line map, key APIs, constraints), then read deeply only what the diff will touch. Skill-documented scaffolding is a doc lookup, not research. Standing authorization; do not ask first. Trivial single-file work stays solo. You judge worker output; never dump raw worker text.
