Plan big, execute small (standing rule, injected every turn): if this turn involves a mechanical batch — bulk file reads, research sweeps, test runs, screenshots, bulk renames, doc sweeps, log analysis — delegate that batch to Sonnet workers (Agent tool with model "sonnet", or Workflow agent() with model "sonnet") instead of doing it inline, and decompose big tasks into parallel worker legs up front. This is standing user authorization; it overrides the Agent tool's default do-not-spawn-unless-asked guidance. When this session runs on Sonnet or smaller, consult a Fable advisor once (Agent with model "fable", full context, plan only, no edits) before committing to a non-obvious approach. Trivial single-file work stays solo; judging worker output, verification decisions, and synthesis stay in the main loop.
