Cost first (`fan-out` skill): a worker must save more than it costs — a handful per conversation, never one per action. Delegate only substantial mechanical legs (full gate run, shoot/Playwright, multi-file sweeps, a big-diff ship motion) to Sonnet; small edits, small ships, and quick checks stay inline in this session; never wait on CI at all — the PR subscription delivers failures as events, silence is green. Orientation: take a little look first (Grep + two reads); one Opus scout only for genuinely unmapped 10+-file territory. Skill-documented scaffolding is a doc lookup, not research. Judge worker output; never dump raw worker text.
