import type { GameContextContent } from "@jgengine/core/runtime/gameContext";

// Gem Cascade is a pure board game: no scene entities, objects, or items.
// The whole game lives in the deterministic board store, so content is empty.
export const content: GameContextContent = {};
